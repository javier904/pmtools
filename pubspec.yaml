name: agile_tools
description: "Agile Tools - Eisenhower Matrix & Estimation Room for collaborative team estimation"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.9.0
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.1

  # Auth
  google_sign_in: ^6.2.2

  # Charts
  fl_chart: ^0.69.2

  # Utils
  intl: ^0.20.1
  uuid: ^4.5.1
  collection: ^1.19.1

  # HTTP (per inviti email)
  http: ^1.2.2
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0

  # URL Launcher
  url_launcher: ^6.2.6

  # Preferences
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
